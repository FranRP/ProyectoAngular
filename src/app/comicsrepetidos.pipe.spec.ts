import { ComicsrepetidosPipe } from './comicsrepetidos.pipe';

describe('ComicsrepetidosPipe', () => {
  it('create an instance', () => {
    const pipe = new ComicsrepetidosPipe();
    expect(pipe).toBeTruthy();
  });
});
