import { BuscarcomicPipe } from './buscarcomic.pipe';

describe('BuscarcomicPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarcomicPipe();
    expect(pipe).toBeTruthy();
  });
});
